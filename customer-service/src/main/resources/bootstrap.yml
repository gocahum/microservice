spring:
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:6061
      username: root
      password: s3cr3t
      #enabled: true
  boot:
    admin:
      client:
        url:
        - http://localhost:6086
