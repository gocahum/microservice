server:
  port: 6061
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gocahum/microservice.git
          search-paths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          
  security:
    user:
      name: root
      password: s3cr3t
          
          